"""
神经网络核心概念总结

1. 核心特性
   - 自动学习能力：神经网络能够自动从数据中学习合适的权重参数
   - 学习流程：
        * 本章重点：神经网络识别时的处理机制
        * 下一章重点：如何从数据中学习权重参数

2. 网络层级结构
   - 输入层：网络最左侧层，接收原始数据
   - 中间层/隐藏层：位于输入输出层之间，进行特征变换
   - 输出层：网络最右侧层，生成最终结果

3. 感知机与激活函数
   - 感知机公式演进：
        y = h(b + w1x1 + w2x2)
        h(x) = { 0 (x ≤ 0), 1 (x > 0) }  # 阶跃函数
        h（x）函数会将输入信号的总和转换为输出信号，这种函数一般称为激活函数（activation function）

   - 激活函数核心作用：
        a = b + w1x1 + w2x2  # 计算加权输入和偏置的总和
        y = h(a)              # 通过激活函数转换输出

        * 连接感知机和神经网络的桥梁
        * 引入非线性变换能力

4. 感知机与神经网络区分
   | 概念         | 定义               | 激活函数       | 网络结构   |
   |--------------|--------------------|----------------|------------|
   | 朴素感知机   | 单层网络模型       | 阶跃函数       | 单层结构   |
   | 多层感知机   | 真正的神经网络     | 平滑函数(如sigmoid)| 多层结构   |

5. 激活函数类型（后续章节详解）
   - 阶跃函数：朴素感知机使用
   - sigmoid函数：神经网络常用平滑函数
   - ReLU等：现代深度学习常用函数

6. 关键计算流程
   输入信号 → [加权求和 + 偏置] → [激活函数] → 输出信号
               (线性变换)        (非线性变换)
"""